// Credentials are configured via JCasC (jenkins.yaml)
// This script just logs confirmation
println "[INIT] Credentials configured via JCasC (jenkins.yaml)"
println "[INIT] Check: Manage Jenkins > Credentials > System > Global"
